{
  "$jsonSchema": {
    "bsonType": "object",
    "title": "Character Object Validation",
    "required": [
      "name",
      "xid",
      "factionID",
      "serverID",
      "lastLogin",
      "battleRank"
    ],
    "properties": {
      "name": {
        "bsonType": "string",
        "description": "'name' must be a string and is required"
      },
      "xid": {
        "bsonType": "long",
        "minimum": 0,
        "description": "'xid' must be an integer above 0 and is required"
      },
      "factionID": {
        "bsonType": "int",
        "minimum": 1,
        "maximum": 10,
        "description": "'factionID' must be an integer in the range (1, 10) and is required"
      },
      "serverID": {
        "bsonType": "int",
        "minimum": 1,
        "maximum": 100,
        "description": "'serverID' must be an integer in the range (1, 100) and is required"
      },
      "outfitID": {
        "bsonType": ["null", "int"],
        "minimum": 0,
        "description": "'outfitID' must be an integer above 0"
      },
      "outfitTag": {
        "bsonType": ["null", "string"],
        "pattern": "^.+$",
        "description": "'outfitTag' must be a non-empty string"
      },
      "lastLogin": {
        "bsonType": "date",
        "description": "'lastLogin' must be a date and is required"
      },
      "battleRank": {
        "bsonType": "int",
        "minimum": 0,
        "maximum": 120,
        "description": "'battleRank' must be an integer in the range (1, 120) and is required"
      },
      "charItems": {
        "bsonType": "array",
        "description": "'items' is an array that may be empty",
        "items": {
          "bsonType": "object",
          "required": ["xid", "lastRecorded"],
          "properties": {
            "xid": {
              "bsonType": "int",
              "minimum": 0,
              "description": "'xid' must be an integer above 0 and is required"
            },
            "lastRecorded": {
              "bsonType": "date",
              "description": "'date' must be a date and is required"
            }
          }
        }
      }
    }
  }
}
